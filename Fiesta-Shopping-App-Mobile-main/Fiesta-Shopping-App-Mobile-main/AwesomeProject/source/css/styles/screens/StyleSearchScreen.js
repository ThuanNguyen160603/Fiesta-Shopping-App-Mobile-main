import { View, Text, StyleSheet } from 'react-native'
import React from 'react'

export const StyleSearchScreen = StyleSheet.create({
    container: {
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'space-between',
        marginTop: 30
    },
    listCat: {
        margin: 3.5, padding: 3.5,
        borderRadius: 20, paddingLeft: 15,
        paddingRight: 15, borderWidth: 0.3
    }
})
