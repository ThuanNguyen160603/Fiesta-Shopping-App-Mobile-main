import Icon from "react-native-vector-icons/MaterialIcons";
import React from "react";
export const MetarialIcon = ({ name, size, color }) => {
    {
        return (
            <Icon name={name} color={color} size={size} />
        )
    }
}
