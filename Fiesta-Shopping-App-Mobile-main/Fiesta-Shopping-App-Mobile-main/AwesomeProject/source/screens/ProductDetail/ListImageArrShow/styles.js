import { StyleSheet } from "react-native";
import { Dimensions } from "react-native";
import { POPPINS_FONT } from "../../css/theme/Theme";

const StyleSlideShow=StyleSheet.create({
      backgroundImage: {
        flex: 0.5,
        resizeMode: 'cover',
        justifyContent: 'center',
      },
      animatedContainer: {
      backgroundColor:'red',
      flex:1
      },
    // itemColor
    });
export default StyleSlideShow;